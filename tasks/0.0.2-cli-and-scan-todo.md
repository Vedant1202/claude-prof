# 0.0.2-alpha (CLI polish + `scan`) — Task list

Spec: `.plans/0.0.2-cli-and-scan-spec.md` · Plan: `tasks/0.0.2-cli-and-scan-plan.md`
Branch: `feat/0.0.2-cli-and-scan` (off `main`)
Legend: `[ ]` todo · `[~]` in progress · `[x]` done · ⟂ = checkpoint (stop & confirm)

## Phase 1 — Foundation

- [x] T1 Command registry (`cli/src/registry.ts`) + dispatch refactor in `main()`; behavior-preserving
- [x] ⟂ Checkpoint A — existing CLI suite green unchanged; build + lint green

## Phase 2 — Help & stream hygiene

- [x] T2 Per-subcommand help (fix global `--help` swallow) + `cprof help <cmd>`
- [x] T3 `parseCommonFlags` + status logs→stderr + `--quiet` (no color); init/refresh/install
- [x] ⟂ Checkpoint B — per-command help works; status on stderr; `--quiet` works; suite green

## Phase 3 — Uniform `--json`

- [ ] T4 JSON envelope helper + `--json` on init/refresh/install (stdout = JSON only)
- [ ] T5 Normalize validate/diff/profiles to the envelope
- [ ] ⟂ Checkpoint C — all six commands emit the consistent envelope

## Phase 4 — `cprof scan`

- [ ] T6 line:col enrichment in core `leak-check.ts` (additive, back-compat)
- [ ] T7 `cprof scan <file…>` command (exit 0/3/2/1; human + `--json`); register in table
- [ ] ⟂ Checkpoint D — scan gates a seeded-secret file (exit 3), passes clean (exit 0); suites green

## Phase 5 — Completions

- [ ] T8 `cprof completion bash|zsh|fish` generated from the table; snapshot tests

## Phase 6 — Docs & wrap-up

- [ ] T9 Docs scan page + pre-commit/Action recipes; README + command tables; `--json`/`--quiet` notes
- [ ] ⟂ Checkpoint F — `corepack pnpm verify` green; spec §7 met, §8 boundaries respected; PR opened

## Owner-run (out of build scope)

- [ ] Merge `docs-followups` (`.gitattributes` LF fix) before/with this PR — Windows CI guard
- [ ] Version bump → `0.0.2-alpha.0` + publish (release runbook), after 0.0.2 milestone scope is final
